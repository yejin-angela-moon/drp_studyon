platform :ios, '16.0'

target 'StudyOn' do
  use_frameworks!

  # Add the Firebase pods that you need, skipping unnecessary tests
  pod 'Firebase/Analytics', :testspecs => :none
  pod 'Firebase/Auth', :testspecs => :none
  pod 'Firebase/Firestore', :testspecs => :none

  # Add other pods if needed
end
